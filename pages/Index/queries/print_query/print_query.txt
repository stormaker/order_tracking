SELECT style,school,sum(QTY)AS SUM,PrintingMarkCompleted FROM ordinfo WHERE PrintingMarkCompleted BETWEEN "{{Start.formattedDate}}" AND "{{End.formattedDate}}" GROUP BY PrintingMarkCompleted ORDER BY PrintingMarkCompleted;